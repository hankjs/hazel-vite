{
  "extends": "./tsconfig.app.json",
  "include": ["src/**/*.ts", "tests/**/*.ts"],
  "exclude": ["node_modules", "dist"],
  "compilerOptions": {
    "composite": true,
    "types": [
      "node"
    ]
  }
}